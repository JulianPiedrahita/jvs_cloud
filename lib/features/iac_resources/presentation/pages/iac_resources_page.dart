import 'package:flutter/material.dart';

/// IaC Resources page
class IaCResourcesPage extends StatelessWidget {
  const IaCResourcesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('IaC Resources')),
      body: const Center(child: Text('IaC Resources Page')),
    );
  }
}